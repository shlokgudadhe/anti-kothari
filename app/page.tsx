import Link from "next/link"; import { currentProfile } from "@/lib/auth"; import { appName } from "@/lib/config";
export default async function Home(){const p=await currentProfile();return <><div className="nav"><div className="shell navin"><div className="brand">{appName}<span>.</span></div><Link className="btn" href={p?"/dashboard":"/login"}>{p?`Open ${appName}`:"Sign in"}</Link></div></div><main className="shell hero"><p className="eyebrow">Shared expenses, without the noise</p><h1>Keep the good times. Split the bills.</h1><p>{appName} makes it easy for your group to track shared spending, understand who owes what, and settle with fewer payments.</p><Link className="btn" href="/login">Get started with Google</Link></main></>}

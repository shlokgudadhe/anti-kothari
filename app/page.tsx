import Link from "next/link"; import { currentProfile } from "@/lib/auth"; import { appName } from "@/lib/config";
export default async function Home(){const p=await currentProfile();return <><div className="nav"><div className="shell navin"><div className="brand">{appName}<span>.</span></div><Link className="btn secondary" href={p?"/dashboard":"/login"}>{p?`Open ${appName}`:"Sign in"}</Link></div></div><main className="shell hero"><div className="hero-copy"><p className="eyebrow">The group tab, without the group chat chaos</p><h1>Good nights out. Clear tabs after.</h1><p>{appName} keeps the who-paid-what part of friendship simple: one place for expenses, balances, and the moment everyone can finally say “settled.”</p><div className="hero-actions"><Link className="btn" href="/login">Start with Google <span aria-hidden>↗</span></Link><Link className="btn secondary" href="#how-it-works">See how it works</Link></div><p className="hero-note">Built for small groups · private by default · no awkward reminders</p></div></main><section id="how-it-works" className="feature-grid shell"><article><p className="eyebrow">01 / Add</p><h2>Put the tab in one place.</h2><p>Log an expense in seconds. Everyone sees the same running total.</p></article><article><p className="eyebrow">02 / See</p><h2>Know where you stand.</h2><p>A calm, honest balance replaces the spreadsheet archaeology.</p></article><article><p className="eyebrow">03 / Settle</p><h2>Close the loop.</h2><p>Record the payment and get back to the part of the night you actually remember.</p></article></section></>}

import Link from "next/link"; import { currentProfile } from "@/lib/auth"; import { appName } from "@/lib/config";
export default async function Home(){const p=await currentProfile();return <><div className="nav"><div className="shell navin"><div className="brand">{appName}<span>.</span></div><Link className="btn secondary" href={p?"/dashboard":"/login"}>{p?"Home":"Sign in"}</Link></div></div><main className="shell hero"><div className="hero-copy"><p className="eyebrow">{appName}</p><h1>Split bills with your group.</h1><p>Add an expense, choose who’s included, and see the balance.</p><div className="hero-actions"><Link className="btn" href="/login">Sign in with Google</Link></div></div></main></>}

import Link from "next/link"; import { currentProfile } from "@/lib/auth"; import { appName } from "@/lib/config";
export default async function Home(){const p=await currentProfile();return <><div className="nav"><div className="shell navin"><div className="brand">{appName}<span>.</span></div><Link className="btn secondary" href={p?"/dashboard":"/login"}>{p?`Open ${appName}`:"Sign in"}</Link></div></div><main className="shell hero"><div className="hero-copy"><p className="eyebrow">For flatmates, trips, and the friend group</p><h1>Who paid? Sorted.</h1><p>{appName} keeps group spending in one place. Add the bill, pick the people, and see exactly what everyone owes.</p><div className="hero-actions"><Link className="btn" href="/login">Sign in with Google <span aria-hidden>↗</span></Link><Link className="btn secondary" href="#how-it-works">How it works</Link></div><p className="hero-note">Free for your group · private by default</p></div></main><section id="how-it-works" className="feature-grid shell"><article><p className="eyebrow">Add a bill</p><h2>Type it once.</h2><p>Record dinner, rent, groceries, or the auto ride before you forget.</p></article><article><p className="eyebrow">Check the total</p><h2>See who owes.</h2><p>Your group gets one shared answer instead of five different calculations.</p></article><article><p className="eyebrow">Settle up</p><h2>Mark it done.</h2><p>Record a payment and keep the group balance accurate.</p></article></section></>}

import Link from "next/link"; import { Nav } from "@/components/nav"; import { requireApproved } from "@/lib/auth"; import { createClient } from "@/lib/supabase/server";
export default async function Dashboard(){const me=await requireApproved();const s=await createClient();const {data:totals}=await s.rpc("dashboard_totals");const groups=(totals||[]).map((x:any)=>({id:x.group_id,name:x.group_name}));let owed=0,owing=0;(totals||[]).forEach((x:any)=>{owed+=Number(x.owed||0);owing+=Number(x.owing||0)});const net=owed-owing;const firstGroup=groups[0];return <><Nav/><main className="shell page"><div className="sectionhead"><div><p className="eyebrow">Home</p><h1>{me.full_name?.split(" ")[0]||"Hi"}</h1></div><Link className="btn" href="/groups/new">New group</Link></div><section className="balance-hero"><div><p className="eyebrow">Balance</p><h2>{net>=0?"₹":"−₹"}{Math.abs(net).toFixed(2)}</h2><p className="muted">{net>0?"You are owed":net<0?"You owe":"Settled"}</p></div>{firstGroup?<Link className="btn secondary" href={`/groups/${firstGroup.id}`}>Add expense</Link>:<Link className="btn secondary" href="/groups/new">Create group</Link>}</section><div className="grid"><div className="card stat"><span className="eyebrow">Owed to you</span><strong className="amount positive">₹{Math.max(net,0).toFixed(2)}</strong></div><div className="card stat"><span className="eyebrow">You owe</span><strong className="amount negative">₹{Math.max(-net,0).toFixed(2)}</strong></div><div className="card stat"><span className="eyebrow">Groups</span><strong>{groups.length}</strong></div></div><section style={{marginTop:38}}><div className="sectionhead"><h2>Groups</h2>{groups.length>0&&<Link className="btn secondary" href="/groups/new">New group</Link>}</div><div className="card">{groups.length?<div>{groups.map((g:any)=><div className="group-card" key={g.id}><div className="row"><div className="group-mark">{g.name.slice(0,1).toUpperCase()}</div><div><strong>{g.name}</strong><div className="muted">Expenses and settlements</div></div></div><Link className="btn secondary" href={`/groups/${g.id}`}>Open</Link></div>)}</div>:<div className="empty"><h3>No groups yet</h3><p>Create a group to get started.</p><Link className="btn" href="/groups/new">Create group</Link></div>}</div></section></main></>}

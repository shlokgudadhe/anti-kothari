import { createClient } from "@/lib/supabase/server"; import { redirect } from "next/navigation";
export default function Login(){async function login(){"use server";const supabase=await createClient();const {data}=await supabase.auth.signInWithOAuth({provider:"google",options:{redirectTo:`${process.env.NEXT_PUBLIC_SITE_URL || "http://localhost:3000"}/auth/callback`}});redirect(data.url!)}return <main className="auth"><div className="brand">tally<span>.</span></div><div className="card"><h1>Welcome back</h1><p className="muted">Use your Google account to join your friend group.</p><form action={login}><button className="btn">Continue with Google</button></form></div></main>}

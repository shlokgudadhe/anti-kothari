import { createClient } from "@/lib/supabase/server"; import { redirect } from "next/navigation"; import { appName, isSupabaseConfigured } from "@/lib/config"; import { SubmitButton } from "@/components/submit-button";
export default function Login(){async function login(){"use server";if(!isSupabaseConfigured)return;const supabase=await createClient();const {data,error}=await supabase.auth.signInWithOAuth({provider:"google",options:{redirectTo:`${process.env.NEXT_PUBLIC_SITE_URL || "http://localhost:3000"}/auth/callback`}});if(error||!data.url)return;redirect(data.url)}return <main className="auth"><div className="brand">{appName}</div><div className="card"><p className="eyebrow">Sign in</p><h1>Welcome back</h1><p>Use your Google account.</p>{isSupabaseConfigured?<form action={login}><SubmitButton pendingLabel="Opening Google…">Continue with Google</SubmitButton></form>:<div className="notice">Sign-in is not configured yet.</div>}</div></main>}

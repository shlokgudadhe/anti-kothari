import { Nav } from "@/components/nav"; import { currentProfile } from "@/lib/auth"; import { redirect } from "next/navigation";
export default async function Pending(){const p=await currentProfile();if(!p)redirect("/login");if(p.status==="approved")redirect("/dashboard");return <><Nav/><main className="shell page"><div className="card" style={{maxWidth:620,margin:"50px auto"}}><h1>{p.status==="removed"?"Access removed":"Waiting for approval"}</h1><p className="muted">{p.status==="removed"?"An admin has removed your access. Contact them if this was a mistake.":"Your account is ready. An admin just needs to approve you before you can see shared expenses."}</p></div></main></>}

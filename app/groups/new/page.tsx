import { Nav } from "@/components/nav"; import { createGroup } from "@/app/actions"; import { requireApproved } from "@/lib/auth";
export default async function NewGroup(){await requireApproved();return <><Nav/><main className="shell page"><div className="card" style={{maxWidth:560,margin:"20px auto"}}><h1>Create a group</h1><p className="muted">Trips, flatmates, dinner club — whatever brings people together.</p><form className="form" action={createGroup}><label>Group name<input name="name" placeholder="Goa trip 2026" required/></label><button className="btn">Create group</button></form></div></main></>}
